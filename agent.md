# Pet Care 项目协作规范

## 项目信息

- 中文宠物洗护店静态单页，核心目标是引导用户预约洗护。
- 页面入口：`index.html`。
- 技术栈：HTML5、CSS3、原生 JavaScript。
- CSS 和 JavaScript 内嵌于 `index.html`。
- 不依赖后端、构建工具、外部字体、外部图片或第三方库。
- 页面支持离线打开，并通过 GitHub Pages 发布。
- 预约表单仅作前端演示，不发送或保存用户信息。
- 主要锚点：`#services`、`#process`、`#reviews`、`#booking`。

除非用户明确要求，不拆分单文件结构、不引入框架或外部依赖。

## 用户偏好

### 视觉风格

- 温暖治愈、奶油色、手绘感、亲切柔软。
- 焦糖橙作为行动色，鼠尾草绿作为辅助色。
- 使用大圆角、柔和阴影、细描边，避免尖锐生硬。
- 可使用爪印、泡泡、Emoji 和 CSS 插画，但装饰不能造成杂乱。
- 页面要舒展、有呼吸感，避免拥挤、局促和信息堆叠。
- 模块占用空间过大时，优先压缩或渐进展示。
- 桌面端和移动端都不得出现横向溢出。

### 文案风格

- 自然、清晰、友好，保留品牌温度。
- 避免过度卖萌、黏腻或含义模糊的表达。
- 功能区域优先使用简洁、直接的字段名称。
- 已确认的首屏标题为“洗去小邋遢，焕新好状态”。
- 局部文案修改必须严格限制在用户指定区域。

### 产品与交互

- 预约转化优先，保持信息层级清晰和预约路径简短。
- 表单字段应易懂，必填状态、错误提示和成功反馈必须明确。
- 备注为选填，最多 200 字，支持实时计数和提交后重置。
- 默认预约日期为明天，默认时间为 `09:00`；提交重置后恢复该默认值。
- 用户曾明确选择“预约服务”在“宠物类型”之前。
- 顾客评价使用紧凑的单卡片轮播，默认每 5 秒自动切换。
- 轮播支持按钮、圆点、键盘和移动端滑动。
- 悬停、焦点进入或页面不可见时暂停轮播。
- 尊重 `prefers-reduced-motion`，减少动态效果时关闭自动播放和动画。
- 控件触摸区域不小于约 44px，键盘焦点清晰可见。

## 修改规则

- 严格遵守用户指定范围，不顺手修改或重构无关内容。
- 保留用户已有的未提交改动和未跟踪文件。
- 简单局部修改不自动创建设计规格或实施计划文件。
- 使用 `apply_patch` 修改项目文件。
- 不使用 Shell 重定向、临时脚本或 Python 写入文件。
- 读取中文文件时使用 UTF-8，避免 PowerShell 默认编码造成乱码。
- 除非用户明确要求，不增加外部资源、依赖或构建流程。

## Git 操作规则

- 代码修改由代理完成，提交和推送默认由用户手动操作。
- 未经明确授权，不执行 `git add`、`git commit`、`git push` 或创建 PR。
- 只有用户明确要求“合并”后才可以执行本地合并。
- 合并授权不包含推送授权。
- 不执行未经授权的历史改写、分支删除或强制推送。

修改前必须检查：

```powershell
git status --short --branch
git branch --show-current
git log --oneline --decorate -5
git worktree list